"use client";
export default function ChatTextArea({ handleSubmit }) {
  <form className="flex justify-center" onSubmit={handleSubmit}>
    <textarea className="bg-indigo-100 max-w-2xl p-2"></textarea>
  </form>;
}
